describe('employee data', function () {
     it('should retrieve the employees', function () {
          var word = 'hello';

          expect(word).to.equal('hello');
          expect(word).to.not.equal('zzz');
     })
});