/*import { describe, expect, it } from 'angular2/testing.d'*/
import { EmployeeDataService } from '../app/employees-data.service';

//suite
describe('modal component', () => {
    //specs
    it('should output the employee and delete it from the array', () => {
        expect(true).toBe(true);
    });
});
